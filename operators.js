let left_operand = 20;
let right_operand = 12;

let ans = '+';
let ans1 = '-';
let ans2 = '*';
let ans3 = '/';



if (ans ==  '+') {
	let add = left_operand + right_operand;
	console.log(add);
}
if (ans1 ==  '-') {
	let subtract = left_operand - right_operand;
	console.log(subtract);
}
if (ans2 ==  '*') {
	let product = left_operand * right_operand;
	console.log(product);
}

if (ans3 ==  '/') {
	let divide = left_operand / right_operand;
	console.log(divide);
}